import React from "react";
import ReactDOM from "react-dom";
import App from "./components/app";

ReactDOM.render(<App />, document.getElementById("root")); //render 1м параметром принимает именно КОМПОНЕНТ, а не переменную App
